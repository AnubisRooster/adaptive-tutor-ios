// Jest setup shared across all test files.
// React Native Testing Library matchers are bundled into the library as of v12.4+,
// so no extra matcher import is required here. Add global mocks/polyfills below
// as the app grows (e.g. expo-secure-store, expo/fetch).
