# App Store Connect — Listing Kit

Everything below is ready to paste into App Store Connect. Items marked **(in ASC)** are
answered in the portal UI, not in the app. Review all copy before submitting — it can only be
edited after approval until the first release goes live.

- App name: **Adaptive Tutor** (matches `app.json` → App Store display name)
- Category: **Education** · Secondary: none
- Language: English (US)
- Price: Free (no IAP, no ads)
- **Synced source of truth:** all copy + age rating + review info live in
  [`store.config.json`](../store.config.json) (EAS Metadata schema). Edit that file, then run
  `npm run metadata:push`. The dashboard fields documented below mirror it.

## Subtitle (30 chars max)

```
Local AI tutor & quiz master
```

## Promotional text (170 chars max)

```
Learn any subject with a private on-device AI tutor, adaptive quizzes, progress tracking, and your own imported notes. No account required.
```

## Keywords (100 chars max, comma-separated, no spaces)

```
ai tutor,study,quiz,flashcards,learning,education,llm,homework,knowledge,notes,language
```

That's 85 chars — safe margin.

## Short description

```
Adaptive Tutor is a private, local-first Study Buddy that teaches Philosophy, Psychology, AI, Physics, Coding & more. It learns your level, quizzes you, and tracks streaks, XP, and mastery — all on your device.
```

## Long description (App Store)

```
Adaptive Tutor is a private AI study buddy that turns any subject into a personal tutoring session.

LEARN YOUR WAY
• Adaptive lessons that adjust to your mastery and level, from Remember to Create.
• A built-in lesson plan per topic, so you always know the next step.
• On-device tutoring: download a small model once and learn fully offline.

QUIZ THAT ADAPTS
• 5-question sessions that grade your short answers and zero in on gaps.
• Bloom’s-taxonomy progression: build understanding from foundations up.
• Mastery tracking, streaks, XP, levels, and achievements to keep you going.

SEARCH & INGEST
• Search every note and saved source instantly.
• Add material by URL; the app stores and retrieves it on your device.

PRIVATE BY DESIGN
• No account, no ads, no tracking, no servers.
• Everything — profiles, answers, progress, and your imported notes — stays in the app's local database and secure storage.
• Optional Cloud mode is bring-your-own-key (OpenRouter) and requires your consent.
• Lessons and quizzes are generated by an AI model; content is a study aid, not professional advice (see in-app Privacy & AI disclosure).

SELECTED FOR
Anyone 13+ who wants a focused, judgmental-free study partner for Philosophy, Psychology, AI, Physics, Coding, Biology, Chemistry, and more.
```

## Placeholders to replace before `eas metadata:push`

`store.config.json` ships with working copy but a placeholder contact — EAS validation won't flag
it, Apple's review team or the page itself will:

- `apple.review.firstName` / `lastName` — real App Store Connect contact.
- `apple.review.email` — reachable inbox (currently `privacy@adaptivetutor.example`).
- `apple.review.phone` — real formatted phone (`+1 …`).

Privacy URLs are resolved: [`docs/privacy-policy.html`](../docs/privacy-policy.html) and
[`docs/privacy-choices.html`](../docs/privacy-choices.html) are deployed to GitHub Pages by
`.github/workflows/privacy-pages.yml` → `https://anubisrooster.github.io/adaptive-tutor-ios/…`.
Before relying on them, one manual step: enable **Settings → Pages → Source "GitHub Actions"** in
the repo. If the repo owner/name differs, update the two URLs in `store.config.json`.

`npm run metadata:push` also needs an App Store Connect API key (`eas init`/`eas credentials` +
ASC key) and the build already created in App Store Connect.

## App Preview / demo notes

- No sign-up; first launch = create a profile → guided setup.
- Without a downloaded model or API key the app runs in **Preview mode**, so a reviewer can
  exercise lessons and quizzes immediately.

---

## Age rating (in ASC — rating questionnaire)

Answer discretely per the questionnaire; the resulting badge is expected to be **4+**:

| Question | Answer |
| --- | --- |
| Hmm — all content/graphic boxes | None |
| Gambling | None / No |
| Medical/treatment references | No |
| User-generated content | No (learners see only their own + AI content) |
| Installed apps/links | No |
| Purchase/subscription | No |

Product page should still set expectations for teens: the in-app Privacy & AI disclosure advises parental guidance under 18 and the app targets 13+.

## App Privacy (in ASC — privacy details)

The app collects nothing remotely. Policy: **Data Not Collected** for most categories. Two honest exceptions to document under **User Content** (we store locally and, in Cloud mode, transmit prompts you explicitly agree to):

| Data type | Collected | Linked to user | Used for tracking |
| --- | --- | --- | --- |
| User content — prompts & answers | Yes (stored locally; sent to OpenRouter only in Cloud mode with consent) | No | No |
| User content — imported notes (URLs) | Yes (stored locally) | No | No |
| Identifiers — API key | Yes (only in SecureStore/Keychain, never leaves device) | No | No |
| All other categories | No | — | — |

Must also toggle: **Data Used to Track You = No**. Encryption: `ITSAppUsesNonExemptEncryption = false` is already set, so the export-compliance exemption applies — answer "No" to the encryption question on submission.

## Screenshots (1206×2622 @3x — matches current captures)

Recommended order (primary = #1):

| # | File | Caption |
| --- | --- | --- |
| 1 | `assets/screenshots/06-preview-tutor.png` | Tutoring session |
| 2 | `assets/screenshots/05-learn.png` | Learn workspace |
| 3 | `assets/screenshots/07-quiz.png` | Adaptive quiz |
| 4 | `assets/screenshots/10-progress.png` | Progress & mastery |
| 5 | `assets/screenshots/14-search-results.png` | Search your notes |
| 6 | `assets/screenshots/03-setup-models.png` | Private on-device models |
| 7 | `assets/screenshots/12-privacy.png` | Privacy & AI disclosure |

All are 1206×2622 (standard 6.7″/6.9″ slot). Trim/enhance as desired; captions optional but improve conversion. If you want the 6.5″ and 5.5″ slots filled, re-capture on the matching simulator sizes.

## Review notes (in ASC, "App Review Information")

- Sign-in: none required.
- Test steps: 1) Launch → Create a profile ("Maya"). 2) Setup → "Skip for now — browse in Preview mode". 3) Learn tab → "Teach me" (preview turn appears). 4) "Start a quiz" → answer → graded. 5) Header 🔍 → search.
- Contact: privacy@adaptivetutor.example
- Notice: prompts/answers are sent to OpenRouter only if the user enables Cloud mode with their own key.

## Release checklist

- [ ] Age rating questionnaire (above) in ASC
- [ ] App Privacy answers (above) in ASC
- [ ] Subtitles/keywords/promo/description pasted
- [ ] Screenshots uploaded + ordered (playbook above)
- [ ] App Review notes filled
- [ ] `eas submit --platform ios` after a production build; software: submission uses non-exempt=false